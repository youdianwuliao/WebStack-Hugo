# 品牌方案（站名/副标/印章）

> 本站当前启用方案与备选方案，改站名时按此文档执行。

## 当前方案（启用中）：集思阁

**集思阁 —— 搜罗天下巧器，汇于一阁**

| 项 | 内容 |
| --- | --- |
| 站名 | 集思阁 |
| 副标/Slogan | 搜罗天下巧器，汇于一阁 |
| 首页印章字 | 思 |

## 备选方案

### 万卷楼

**万卷楼 —— 古今万卷，一网观澜**

| 项 | 内容 |
| --- | --- |
| 站名 | 万卷楼 |
| 副标/Slogan | 古今万卷，一网观澜 |
| 首页印章字 | 卷 |

> 万卷楼整套改动在 commit `281853e`（含 tag `wanjuanlou-plan`），可 `git revert` 回退的提交 `d22398b` 快速启用。

### 神机阁

**神机阁 —— 百工利器，神机妙用**

| 项 | 内容 |
| --- | --- |
| 站名 | 神机阁 |
| 副标/Slogan | 百工利器，神机妙用 |
| 首页印章字 | 机 |

> 神机阁为 2026-09-05 更换为集思阁前的上一版启用方案，未单独打 tag；如需回退可 `git revert` 本站名提交。

## 改名操作要点

- 印章为单字章，字取站名**中间字**：天工阁→工，神机阁→机，万卷楼→卷
- 全局替换站名：约 453 个 `*.html` + `manifest.webmanifest` + `package.json`
- `index.html` 需单独改三处：h1 站名（约 390 行）、`.brand-tagline` 副标（约 397 行）、品牌印章 SVG `<text>` 内文字（约 394 行）
- 分支 `staticV4`；推送需同时推两个远端：`origin`(GitHub) 与 `gitee`
- 快速启用「万卷楼」方案：`git revert d22398b`（即恢复 `281853e` 的改动），或直接切到 commit `281853e`，再推送双远端
